schema {
    query: Query
    mutation: Mutation
}

type Query {
    # Outputs the purpose of this GraphQL endpoint.
    purpose: String!
}

type Mutation {
    # Registers a new User.
    register(email: String!, password: String!): User
    # Logs a User in, returns a JSONWebtoken.
    login(email: String!, password: String!): String!
}
